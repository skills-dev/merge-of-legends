name: 0-0 Start

on:
  workflow_dispatch:
  workflow_call:
  issues:
    types: [closed]

permissions:
  contents: write
  actions: write
  issues: write

jobs:
  start_exercise:
    name: Start Exercise
    uses: skills/exercise-toolkit/.github/workflows/start-exercise.yml@v0.9.3
    with:
      exercise-title: "Merge of Legends"
      issue-title-prefix: "Quest: "
      intro-message: "Choose your path with Ducky, Mona, or Copilot to begin the Merge of Legends quest."
    secrets: inherit

  post_challenge_picker:
    name: Post challenge picker
    runs-on: ubuntu-latest
    needs: [start_exercise]
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Create comment - pick challenge
        uses: GrantBirki/comment@v2.1.1
        with:
          issue-number: ${{ needs.start_exercise.outputs.issue-number }}
          file: ".github/steps/0-0-start.md"

      - name: Get response templates
        uses: actions/checkout@v6
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Create comment - watching for progress
        uses: GrantBirki/comment@v2.1.1
        with:
          issue-number: ${{ needs.start_exercise.outputs.issue-number }}
          file: exercise-toolkit/markdown-templates/step-feedback/watching-for-progress.md

      - name: Reenable to allow restarting, and enable challenge picker workflow
        run: |
          gh workflow enable "0-0 Start" || true
          gh workflow enable "0-1 Pick" || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Add comment - close with next issue link
        if: github.event_name == 'issues' && github.event.action == 'closed'
        uses: GrantBirki/comment@v2.1.1
        with:
          issue-number: ${{ github.event.issue.number }}
          file: ".github/steps/x-close-with-next-link.md"
          vars: |
            next_issue_url: ${{ needs.start_exercise.outputs.issue-url }}
